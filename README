To install:
```
pip install -r requirements.txt
```

To prep data:
```
make process_dataa
```

To train and evaulate:
```
make train
```

Many parameters are customizable throught command-line arguments if you run
main.py directly. For example, you can run:
```
python src/main.py -v --lambda_u 1 --lambda_n 1000 --hidden_sizes 1000 200 --activation relu
```
